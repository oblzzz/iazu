html,
  body {
    overflow-x: hidden;
  }

  .hero-mobile {
    display: none;
  }

  /* Burger some — nav vira barra horizontal */
  .burger {
    display: none;
  }

  .logo-header {
    padding-left: 1.5rem;
  }

  .nav {
    position: static;
    /* sai do fixed, volta ao fluxo normal */
    width: 100%;
    height: auto;
    background: transparent;
    padding-top: 0;
    transition: none;
  }

  .nav a {
    background-color: transparent;
    letter-spacing: var(--font-space);
    text-transform: uppercase;
    font: var(--serif);
    color: var(--branco);
    opacity: 0.8;
    padding: 16px 40px;
    transition: color .2s, opacity .2s, transform 0.3s ease;
  }

  .nav a:hover {
    color: var(--marrom);
    opacity: .5;
    transform: scale(1.09);
  }

  .nav ul {
    flex-direction: row;
    justify-content: space-evenly;
    max-width: 40rem;
    margin: auto;
    gap: 0;
    padding-left: 0;
  }

  .nav-divider {
    display: block;
    width: 2px;
    background: var(--branco);
    align-self: stretch;
    /* ← pega a altura total do flex */
    opacity: 0.5;
    margin: 0.6rem 0;
    /*  ← afasta das bordas superior e inferior */
  }

  .nav a {
    position: relative;
    display: inline-block;
  }

  .nav a::after {
    content: "";
    position: absolute;
    left: 50%;
    bottom: 13px;
    width: 50%;
    height: 1px;
    background-color: currentColor;
    transform: translateX(-50%) scaleX(0);
    transform-origin: center;
    transition: transform 0.3s ease;
  }

  .nav a:hover::after {
    transform: translateX(-50%) scaleX(1);
  }

  /* ============================================================ 
 * HERO DESKTOP 769
 * ============================================================ */

  .hero {
    /* border: 2px solid fuchsia; */
  }

  .hero-desktop {
    position: relative;
    overflow: hidden;
    /* ESSENCIAL — ancora o cta-desktop */
    display: flex;
    height: 32rem;
    /* border: 5px dotted yellow; */
  }

  /* .hero-desktop{
    display: flex;
    height: 32rem;
    border: 2px solid red;
  } */

  .logo-desk {
    position: relative;
    /* border: 2px solid orange; */
    margin: 3.5rem;
    z-index: 3;
  }

  .logo-desk .logosvg {
    width: clamp(200px, 25vw, 370px);
    /* border: 2px solid green; */
    padding-bottom: 0.5rem;
  }

  .hero-desktop h3 {
    font: var(--serif);
    text-transform: uppercase;
    letter-spacing: var(--font-space);
    color: var(--mostarda);
    font-weight: 100;
    font-size: clamp(1rem, 2.5vw, 3rem);
  }

  /* .hero-desktop .decor img{
    height: 100px;
    border: 2px solid blue;
  } */

  .wire-left {
    display: flex;
    /* aparece no desktop */
    position: absolute;
    bottom: -23rem;
    left: -21rem;
    margin: 0;
    /* zera a margem automática do mobile */
    width: 700px;
    z-index: 1;
    /* width: clamp(180px, 13vw, 230px); */
    /* tamanho próprio no desktop */
  }

  .wire-right {
    display: flex;
    position: absolute;
    top: -23rem;
    right: -21rem;
    margin: 0;
    width: 700px;
    z-index: 1;
  }

  .hands {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    width: clamp(600px, 60vw, 800px);
  }


  /* ============================================================ 
 * CTA HERO DESKTOP
 * ============================================================ */


  .cta-desktop {
    display: flex;
    /* aparece no desktop */
    position: absolute;
    bottom: 3rem;
    left: 2rem;
    margin: 0;
    /* zera a margem automática do mobile */
    width: clamp(220px, 20vw, 240px);
    /* tamanho próprio no desktop */
    z-index: 3;
  }

  .cta-desktop h2 {
    font-size: clamp(1.5rem, 5.5vw, 1.6rem);
  }

  /* ============================================================ 
 * MAIN CONTENT
 * ============================================================ */

  /* .main-content{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
    position: relative;
    padding-top: 3rem;
    min-height: 2000px;
  } */

  .main-content {
    position: relative;
  }

  .par {
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    /* remova o border: 2px solid red */
  }

  .arco {
    position: relative;
    /* volta ao fluxo */
    width: clamp(300px, 40vw, 520px);
    /* margin: auto; */
  }

  .arco1 {
    justify-self: end;
    /* empurra para a direita da coluna */
    margin-right: 4vw;
    /* margem da borda direita */
    margin-top: -17rem;
    /* sobe para sobrepor o card1 */
    z-index: 2;
  }

  .arco2 {
    justify-self: start;
    /* empurra para a direita da coluna */
    margin-left: 4vw;
    margin-top: -11rem;
    z-index: 2;
  }

  .card {
    position: relative;
    z-index: 1;
    padding: 60px;
  }

  .card1 {
    border-radius: 0px 90px 90px 0px;
    margin-right: 2rem;
    padding-bottom: 10rem;
    z-index: 1;
  }

  .card2 {
    border-radius: 90px 0px 0px 90px;
    margin-left: 2rem;
    margin-top: clamp(10rem, 28vw, 35rem);
    margin-bottom: clamp(10rem, 10vw, 35rem);
    z-index: 1;
    /* REMOVA o margin-top: 65rem e o grid-column: 2 */
  }


  /* Decorativos ficam atrás de tudo */
  .img-l,
  .img-r {
    position: absolute;
    z-index: 0;
  }

  .img-l {
    left: -7vw;
    bottom: 2vw;
  }

  .img-r {
    right: -10vw;
    top: 28vw;
  }



  /* ============================================================ 
 * GALLERY
 * ============================================================ */

  .gallery {
    margin-top: 5rem;
    /* padding-top: 5rem; */
    /* background-color: var(--branco); */
  }





  /* Setas da galeria aparecem no tablet/desktop */
  .gallery-prev,
  .gallery-next {
    display: flex;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 50px;
    height: 50px;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.2);
    border: none;
    color: white;
    font-size: 1.2rem;
    cursor: pointer;
    z-index: 10;
    border-radius: 50%;
    backdrop-filter: blur(5px);
    transition: background 0.3s ease;
  }

  .gallery-prev:hover,
  .gallery-next:hover {
    background: rgba(255, 255, 255, 0.35);
  }

  .gallery-prev {
    left: 10px;
  }

  .gallery-next {
    right: 10px;
  }
}